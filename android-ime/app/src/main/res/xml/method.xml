<?xml version="1.0" encoding="utf-8"?>
<!--
  This file tells Android what this IME is and where its settings live.
  It is referenced by the <meta-data android:name="android.view.im"> tag
  in AndroidManifest.xml.
-->
<input-method
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:settingsActivity="com.example.afrikaanskeyboard.SettingsActivity"
    android:isDefault="false">

    <!-- Subtype for English -->
    <subtype
        android:label="@string/subtype_english"
        android:imeSubtypeLocale="en_US"
        android:imeSubtypeMode="keyboard" />

    <!-- Subtype for Afrikaans -->
    <subtype
        android:label="@string/subtype_afrikaans"
        android:imeSubtypeLocale="af_ZA"
        android:imeSubtypeMode="keyboard" />

</input-method>
